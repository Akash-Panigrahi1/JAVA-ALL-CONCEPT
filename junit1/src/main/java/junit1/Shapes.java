package junit1;

public class Shapes {
	public int computeSquareArea(double length) {
		return (int) (length*length);
		}
	public double computeCircleArea(double radius) {
		return  (3.14*radius*radius);
	}
}
